- `make init` — terraform init
- `make fmt` — terraform fmt
- `make validate` — terraform validate
- `make plan` — terraform plan
- `make apply` — terraform apply
- `make lint` — tflint + tfsec
- `make check` — fmt + validate + lint